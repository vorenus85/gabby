/**
 * Az adott felhasználó postjait adja vissza, userId alapján
 * @param {*} objectRepository
 * @returns
 */

export const getPostByUserId = (objectRepository) => {
  return (req, res, next) => {
    return next();
  };
};
