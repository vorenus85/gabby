/**
 * A postok között tudunk keresni, a content-re vagy a felhasználóra, egyezőség esetén megjelenítjük vagy a postot, vagy a felhasználót.
 * @param {*} objectRepository
 * @returns
 */

export const search = (objectRepository) => {
  return (req, res, next) => {
    return next();
  };
};
