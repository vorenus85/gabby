/**
 * Postokat ad vissza id-k alapján a bemenet egy tömb lesz amiben a post id-k lesznek
 *
 * @param {*} objectRepository
 * @returns
 */

export const getPostsById = (objectRepository) => {
  return (req, res, next) => {
    return next();
  };
};
